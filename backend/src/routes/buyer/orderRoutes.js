const r=require("express").Router(),a=require("../../middleware/authMiddleware"),b=require("../../middleware/buyerMiddleware"),c=require("../../controllers/buyer/orderController");r.use(a,b);r.get("/",c.list);r.get("/:id",c.getOne);module.exports=r;
