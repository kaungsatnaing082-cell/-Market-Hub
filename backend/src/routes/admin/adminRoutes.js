const r=require("express").Router(),a=require("../../middleware/authMiddleware"),m=require("../../middleware/adminMiddleware"),c=require("../../controllers/admin/adminController");r.use(a,m);r.get("/me",c.me);r.get("/dashboard",c.dashboard);r.get("/centers",c.centers);r.get("/centers/:id",c.center);r.patch("/centers/:id/status",c.updateCenterStatus);r.post("/centers/:id/warnings",c.addWarning);r.get("/sellers",c.sellers);r.get("/buyers",c.buyers);r.get("/products",c.products);r.patch("/products/:id/status",c.updateProductStatus);r.get("/warnings",c.warnings);r.patch("/warnings/:id",c.updateWarning);r.get("/finance",c.finance);r.get("/agreements",c.agreements);r.post("/agreements",c.createAgreement);r.get("/notifications",c.notifications);r.patch("/notifications/read-all",c.readNotifications);module.exports=r;
