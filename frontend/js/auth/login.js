document.addEventListener("DOMContentLoaded",()=>{document.querySelector("[data-password-toggle]")?.addEventListener("click",e=>{const i=document.getElementById("password");i.type=i.type==="password"?"text":"password";e.currentTarget.textContent=i.type==="password"?"Show":"Hide"});loginForm.onsubmit=async e=>{e.preventDefault();try{const d=await KrestAPI("/auth/login",{method:"POST",body:JSON.stringify({email:email.value.trim(),password:password.value})});KrestStorage.setAuth(d.token,d.user);if(d.user.role==="ADMIN")location.href="/pages/admin/dashboard.html";else if(d.user.role==="SELLER")location.href="/pages/seller/dashboard.html";else location.href="/pages/buyer/dashboard.html"}catch(err){KrestUI.showMessage(message,err.message)}}});
